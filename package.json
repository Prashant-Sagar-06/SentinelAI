{
  "name": "sentinelai",
  "private": true,
  "workspaces": [
    "services/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "docker compose up --build",
    "down": "docker compose down -v",
    "lint": "echo \"Add lint scripts per service\"",
    "verify:e2e": "node scripts/verify-e2e.js"
  },
  "overrides": {
    "d3-color": "^3.1.0"
  }
}
